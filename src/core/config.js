/**
 * 网站配置文件
 */

const config = {
  appName: 'Aleo-Info-Admin',
  appLogo: './icon.png',
  showViteLogo: true,
  logs: [],
}

export const viteLogo = (env) => {
  if (config.showViteLogo) {
    const chalk = require('chalk')
  }
}

export default config
